# Build artifacts — regenerated by .github/workflows/extension-release.yml
# on every push that touches extension/ or tools/pack-crx/.
#
# We keep these out of normal local commits; the Action force-adds them
# (`add_options: -f`) under a bot identity so the public Pages site always
# serves the bytes Chrome will actually validate.
skfilter.crx
update.xml
